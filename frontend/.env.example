# Copy to .env. The AG-UI agent endpoint (your FastAPI backend).
AGENT_URL=http://localhost:8000/

# Where the CopilotKit runtime is mounted (Vite dev-server middleware).
VITE_COPILOTKIT_RUNTIME_URL=/api/copilotkit
